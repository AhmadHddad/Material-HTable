import { createStyles, makeStyles } from "@mui/styles";

const useICTableStyle = makeStyles((theme) =>
  createStyles({
    root:{
      direction:'ltr !important'
    }
  })
);

export default useICTableStyle;
